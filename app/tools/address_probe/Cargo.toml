[package]
name = "address-probe"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
soroban-sdk = { version = "25.1.0", features = ["hazmat-address"] }

[dev-dependencies]
soroban-sdk = { version = "25.1.0", features = ["hazmat-address", "testutils"] }
